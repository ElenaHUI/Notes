## 第一阶段：环境搭建和现有功能调试（优先级：高）

### 需求一：端到端调试上报功能（已实现，需验证）

- [x]  用 curl 命令安装插件到红区机器
- [x]  启动 opencode，输入 `/report` 走通完整上报流程
![[Pasted image 20260727154618.png]]
- [x]  确认服务端收到数据（查看 app-notifier 日志 + `/data/issue-reports/` 目录）
![[Pasted image 20260727160744.png]]

- [x]  确认钉钉群收到通知![[Pasted image 20260727154512.png]]
- [x]  记录并修复发现的问题
	[[bugfix01-没有report接口]]
	[[bugfix02-上报问题时 403 Forbidden]]

### 需求二：确认诊断信息完整性（已实现，需审查）

- [x]  确认收集的信息是否够定位常见端侧问题
- [x]  缺少的关键信息（如 GPU 型号、模型服务端点）按需补充收集逻辑
- [x]  确认脱敏规则是否完善，有无遗漏的敏感信息类型

### 需求五：确认钉钉通知（已实现，需审查）

- [x]  确认通知格式和内容是否满足需求
	- [x] 暂时调整群消息位置[[钉钉群消息]]
- [ ]  未来在通知中添加 web-mgmt 详情页链接（待前端完成后）

---

## 第二阶段：数据库持久化（优先级：高）

### 需求三：服务端数据库开发（未实现，需开发）

- [ ]  在 `app_mgmt` 数据库中创建 `issue_report` 表（文档 4.3.1 有完整 DDL）
- [ ]  新建 `app/services/issue_report_db_service.py`，参考 `app_info_service.py` 的 PyMySQL 模式
    - 实现 `save_to_db(report_data)` — 插入数据库
    - 实现 `list_reports(page, size, status_filter, reporter_filter)` — 分页查询
    - 实现 `get_report(report_id)` — 查询单条详情
    - 实现 `update_status(report_id, new_status)` — 更新状态
- [ ]  修改 `app/routes/issue_routes.py`，在保存文件后同时保存到数据库
- [ ]  新增 3 个 API 路由：
    - `GET /alert/issue/list` — 分页查询列表
    - `GET /alert/issue/<id>` — 查询单条详情
    - `PUT /alert/issue/<id>/status` — 更新状态
- [ ]  用 curl 或 Postman 测试所有 API

---

## 第三阶段：web-mgmt 前端页面（优先级：中）

### 需求四：前端展示页面开发（未实现，需开发）

- [ ]  新建 `src/services/biz/issueReport.ts` — API 服务定义
- [ ]  新建 `src/queries/issueReport.ts` — TanStack Query hooks
- [ ]  新建 `src/pages/app/issue-reports/index.tsx` — 列表页
    - 表格列：报告 ID、上报人、描述、模型、状态、时间
    - 状态筛选 Tabs（open/in_progress/resolved）
    - 分页 + 刷新按钮
    - 点击行跳转详情页
- [ ]  新建 `src/pages/app/issue-reports/[id].tsx` — 详情页
    - 展示完整报告信息
    - 日志内容（等宽字体、可折叠）
    - 配置信息（JSON 格式化、Monaco Editor）
    - 状态管理（Select 切换状态）
    - 返回按钮
- [ ]  `pnpm dev` 启动开发服务器，本地测试

---

## 第四阶段：联调和优化（优先级：中）

- [ ]  完整流程测试：opencode 上报 → 服务端保存（文件+数据库）→ 钉钉通知 → web-mgmt 查看
- [ ]  修复发现的问题
- [ ]  优化用户体验（如钉钉通知中添加 web-mgmt 详情页链接）
- [ ]  代码 review 和清理

---