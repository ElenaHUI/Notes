## DeepSeek-V4-Flash-0731 部署连通性问题修复

**现象**：DGD 部署 DeepSeek-V4-Flash-0731 后，worker 正常运行且模型注册成功（"Model registration succeeded"），但 dynamo-frontend 的 `/v1/models` 始终不识别该模型。

**根因**：分离式部署（prefill/decode 分开）场景下，prefill worker 默认使用 Pod 网络（CNI），而 dynamo-frontend 的服务发现/探活依赖宿主机网络可达。worker 注册时上报的 endpoint 地址是 Pod IP，frontend 从宿主机网络侧探活该地址不通，导致模型无法晋升为可服务状态。

**修复**：在 DGD 的 `SglangPrefillWorker` 配置中增加 `extraPodSpec`，开启 `hostNetwork: true`，并配套设置 `dnsPolicy: ClusterFirstWithHostNet` 和 `hostIPC: true`。开启宿主机网络后，worker 直接使用节点 IP 监听和注册，frontend 探活通过，模型正常晋升到 `/v1/models` 列表，请求恢复正常。

**结论**：分离式推理架构下，prefill/decode worker 必须使用 hostNetwork 模式，以保证与 dynamo-frontend 路由层的网络连通性。后续 DGD 模板需将此作为分离式部署的默认配置。